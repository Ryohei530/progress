<% if @feed_items.any? %>
  <ol class="<%= posts_or_reports %>">
    <%= render @feed_items %>
  </ol>
  <%= paginate @feed_items, params: { controller: yield(:controller), action: yield(:action) } %>
<% end %>